{
  "name": "crownforge-qa-capture",
  "private": true,
  "scripts": {
    "preflight": "node preflight.js",
    "serve": "http-server .. -a 127.0.0.1 -p 4173 -c-1 --silent",
    "test": "playwright test -c playwright.config.js",
    "test:list": "playwright test -c playwright.config.js --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1"
  }
}
